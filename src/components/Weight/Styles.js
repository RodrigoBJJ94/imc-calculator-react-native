import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    textInput: {
        width: '100%',
        borderWidth: 1,
        borderColor: 'black',
        padding: 5,
        borderRadius: 5,
        marginTop: 4,
    },
    block: {
        marginBottom: 10,
    }
});

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    blockTitle: {
        backgroundColor: 'rgb(81, 2, 100)',
        height: 65,
    },
    textTitle: {
        fontSize: 30,
        fontWeight: 'bold',
        textAlign: 'center',
        alignItems: 'center',
        color: '#fff',
        padding: 12,
    },
});

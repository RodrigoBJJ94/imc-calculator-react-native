import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    blockTitle: {
        backgroundColor: 'rgba(105, 9, 176, 1)',
        height: 60,
    },
    textTitle: {
        fontSize: 25,
        textAlign: 'center',
        alignItems: 'center',
        color: '#fff',
        padding: 12,
    },
});

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    block: {
        marginBottom: 15
    },
    imageImc: {
        width: '100%',
        resizeMode: 'contain',
        marginTop: -48,
    }
});
